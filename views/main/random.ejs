<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <h1>Random Users</h1>
    <% people.sort((a,b)=>(a.name.first > b.name.first) ? 1 : ((b.name.first > a.name.first) ? -1 : 0)).forEach(({picture:{large}, name:{first}, name:{last}}) => { %>
        <img src=<%= large %>>
        <p><%= first +' '+ last %></p>
    <%}) %> 
</body>
</html>